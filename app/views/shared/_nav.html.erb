<nav class="navbar navbar-light bg-light">
  <div class="d-flex justify-content-start align-items-center">
    <div> <%= link_to t('.header'), root_path, class: 'navbar-brand' %></div>
    <% if user_signed_in? %>
      <div class="navbar-brand"> <%= link_to t('admin.nav.header'),
                                             admin_tests_path if current_user.admin? %></div>
      </div>
      <div class="col-6 d-flex justify-content-end">
        <%= t('.welcome') %><b><%= current_user.email %></b>&nbsp;|&nbsp;
        <%= link_to t('admin.badges.index.header'), badges_path %>&nbsp;|&nbsp;
        <%= link_to t('.logout'), destroy_user_session_path, method: :delete %>
    <% else %>
      </div>
      <div class="col-6 d-flex justify-content-end">
        <div class="navbar-brand"> <%= link_to t('.sign_up'), new_user_registration_path %> </div>
        <div class="navbar-brand"> <%= link_to t('.sign_in'), new_user_session_path %> </div>
    <% end %>
    </div>
    </nav>
